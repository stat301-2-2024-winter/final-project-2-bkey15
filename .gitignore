# base_fits
data/results/fits_cv/baselines/base_fits.rda

# post-assess cv fits
data/results/post-assess/cv/log_gdp_cv.rda
data/results/post-assess/cv/log_gdppc_cv.rda
data/results/post-assess/cv/log_pop_cv.rda
data/results/post-assess/cv/hrsc_cv.rda

# base_fits_la
data/results/fits_cv/baselines/appendices/base_fits_la.rda

# base_fits_ma
data/results/fits_cv/baselines/appendices/base_fits_ma.rda

# History files
.Rhistory
.Rapp.history

# Session Data files
.RData
.RDataTmp

# User-specific files
.Ruserdata

# RStudio files
.Rproj.user/
*.Rproj

# OAuth2 token, see https://github.com/hadley/httr/releases/tag/v0.3
.httr-oauth

# knitr and R markdown default cache directories
*_cache/
/cache/

# Temporary files created by R markdown
*.utf8.md
*.knit.md

# R Environment Variables
.Renviron

# mac related files
*.DS_Store
**/.DS_Store

# ignore files (likely datasets or graphics) if too large (> 100 MB)
# specify pathway to dataset data/project_dataset.csv
# if all datasets are too large then use data/*.csv (only ignores CSVs)
# if you have other file types then use appropriate extension data/*.rds
# or
# to automatically find and add files over 100MB to the .gitignore you can type 
# the following code in the Terminal:
# find . -size +100M | sed 's|^\./||g' | cat >> .gitignore; awk '!NF || !seen[$0]++' .gitignore

